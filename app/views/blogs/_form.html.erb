<%= form_for @blog do |f| %>

  <%= render 'shared/errors', model: @blog %>

  <%= f.label :content %>
  <%= f.text_area :content %><br>
  <%= f.submit "Send" %>
<% end %>
<p><%= link_to 'Back', :back %></p>
